name: livepoly

x-application: &application
  image: livepoly-server:${IMAGE_TAG:-latest}
  build:
    context: ..
    dockerfile: deploy/Dockerfile.production
  env_file:
    - ../.env
  environment:
    NODE_ENV: production
    PORT: 3002
    DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer:6432/${POSTGRES_DB}
    REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
    INSTANCE_ROLE: server
  restart: unless-stopped
  stop_grace_period: 30s
  networks:
    - application

x-api-healthcheck: &api-healthcheck
  test:
    - CMD-SHELL
    - wget --quiet --output-document=- http://127.0.0.1:3002/health/live >/dev/null 2>&1
  interval: 15s
  timeout: 5s
  retries: 5
  start_period: 30s

x-worker-healthcheck: &worker-healthcheck
  test:
    - CMD-SHELL
    - node -e "const net=require('node:net');const targets=[['pgbouncer',6432],['redis',6379]];let left=targets.length;const fail=()=>process.exit(1);const done=()=>{if(--left===0)process.exit(0)};setTimeout(fail,4000);for(const [host,port] of targets){const socket=net.connect(port,host,()=>{socket.destroy();done()});socket.on('error',fail);}"
  interval: 20s
  timeout: 5s
  retries: 5
  start_period: 30s

services:
  postgres:
    image: postgres:17-alpine
    env_file:
      - ../.env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: --auth-host=scram-sha-256
    command:
      - postgres
      - -c
      - max_connections=50
      - -c
      - shared_buffers=128MB
      - -c
      - effective_cache_size=512MB
      - -c
      - work_mem=4MB
      - -c
      - maintenance_work_mem=64MB
      - -c
      - wal_compression=on
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - 127.0.0.1:5432:5432
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U "$${POSTGRES_USER}" -d "$${POSTGRES_DB}"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    restart: unless-stopped
    stop_grace_period: 60s
    shm_size: 128mb
    networks:
      - data

  pgbouncer:
    build:
      context: ./pgbouncer
      dockerfile: Dockerfile.pgbouncer
    env_file:
      - ../.env
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - 127.0.0.1:6432:6432
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -h 127.0.0.1 -p 6432 -U "$${POSTGRES_USER}" -d "$${POSTGRES_DB}"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      - application
      - data

  redis:
    image: redis:7.4-alpine
    env_file:
      - ../.env
    command:
      - sh
      - -c
      - exec redis-server /usr/local/etc/redis/redis.conf --requirepass "$${REDIS_PASSWORD}"
    volumes:
      - ./redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis-data:/data
    healthcheck:
      test:
        - CMD-SHELL
        - REDISCLI_AUTH="$${REDIS_PASSWORD}" redis-cli ping | grep PONG
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s
    mem_limit: 384m
    restart: unless-stopped
    stop_grace_period: 30s
    networks:
      - application

  migrate:
    <<: *application
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
    command:
      - node
      - dist/scripts/migrate.js
    depends_on:
      postgres:
        condition: service_healthy
    restart: 'no'
    profiles:
      - tools
    networks:
      - data

  api-1:
    <<: *application
    healthcheck: *api-healthcheck
    depends_on:
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_healthy

  api-2:
    <<: *application
    healthcheck: *api-healthcheck
    depends_on:
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    <<: *application
    environment:
      NODE_ENV: production
      INSTANCE_ROLE: worker
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer:6432/${POSTGRES_DB}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
    command:
      - node
      - dist/worker.js
    depends_on:
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck: *worker-healthcheck

  nginx:
    image: nginx:stable-alpine
    environment:
      API_DOMAIN: ${API_DOMAIN:-api.livepoly.site}
      NGINX_ENVSUBST_FILTER: API_DOMAIN
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/${NGINX_TEMPLATE:-http.conf.template}:/etc/nginx/templates/default.conf.template:ro
      - certbot-challenges:/var/www/certbot:ro
      - letsencrypt:/etc/letsencrypt:ro
    depends_on:
      api-1:
        condition: service_healthy
      api-2:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - wget --quiet --spider http://127.0.0.1/nginx-health
      interval: 15s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    stop_grace_period: 30s
    networks:
      - edge
      - application

  certbot:
    image: certbot/certbot:latest
    volumes:
      - certbot-challenges:/var/www/certbot
      - letsencrypt:/etc/letsencrypt
    profiles:
      - tools
    networks:
      - edge

  newrelic-infra:
    image: newrelic/infrastructure-bundle:latest
    env_file:
      - ../.env
    environment:
      NRIA_LICENSE_KEY: ${NEW_RELIC_LICENSE_KEY}
      NRIA_DISPLAY_NAME: ${NEW_RELIC_INFRA_DISPLAY_NAME:-livepoly-aws-ec2}
      NRIA_VERBOSE: ${NEW_RELIC_INFRA_VERBOSE:-0}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    pid: host
    privileged: true
    restart: unless-stopped
    volumes:
      - /:/host:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./newrelic:/etc/newrelic-infra/integrations.d:ro
    profiles:
      - monitoring
    networks:
      - application
      - data

networks:
  edge:
  application:
  data:
    internal: true

volumes:
  postgres-data:
  redis-data:
  certbot-challenges:
  letsencrypt:
