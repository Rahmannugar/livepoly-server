export const NOTIFICATIONS = {
  defaultLimit: 50,
  maxLimit: 100,
} as const;

export const NOTIFICATION_EVENTS = {
  markedRead: 'NotificationMarkedRead',
  markedReadAll: 'NotificationsMarkedRead',
} as const;
